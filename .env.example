# 本地开发环境配置（使用MySQL）
FLASK_ENV=local
DEBUG=True

# MySQL 配置（本地开发）
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=123456
DB_NAME=wjx_survey

# 云端环境配置（使用PostgreSQL）
# 取消注释下面的行并设置你的PostgreSQL连接字符串
# DATABASE_URL=postgresql://user:password@host:port/database

# Flask 配置
SECRET_KEY=wjx_survey_secret_key
